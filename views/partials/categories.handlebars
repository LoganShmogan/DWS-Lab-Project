<nav class="space-y-2">
    <h2 class="text-xl font-semibold mb-2">Categories:</h2>
    <ul class="list-disc list-inside">
        {{#each categories}}
        <li>
            <a href="/filter/{{this}}" class="text-blue-500 hover:text-blue-700">
                {{this}}
            </a>
        </li>
        {{/each}}
    </ul>
</nav>