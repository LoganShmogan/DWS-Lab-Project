<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body class="bg-gray-100">
    {{!-- Top nav --}}
    <div class="p-4 bg-white shadow">
        <a href="/" class="text-blue-500 hover:text-gray-800">Home</a>

        <a href="/logout" class="text-blue-500" style="float:right; margin-right:1rem;">Logout</a>
        <a href="/register" class="text-blue-500 hover:text-gray-800"
            style="float:right; margin-right:2rem;">Register</a>
        <a href="#" class="text-blue-500 hover:text-gray-800" style="float:right; margin-right:2rem;">
            Logged in as {{user}}</a>
    </div>
    <div class="px-4 py-8">
        <div>
            {{{body}}}
        </div>
    </div>

    <h1>Items List</h1>
    <style>
        table, th, td {
            border: 1px solid #000;
            padding: 5rem;
        }
        id > a, button[type=submit] {
            color: white; padding: 4px; text-align: center; text-decoration: none; display: inline-block; border: none; margin: 0 .5rem;
        }
    </style>
    <table class="table">
        <thead>
            <tr>
                <th>id</th>
                <th>Item</th>
                <th>Category</th>
                <th colspan="3">Actions
                    <a href="/items/create" style="color:blue; float:right;">+ Create New</a>
                </th>
            </tr>
        </thead>
        <tbody>
            {{#each items}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.name}}</td>
                <td>{{this.category}}</td>
                <td><a href="/items/{{this.id}}" style="background-color: rgb(93, 123, 179);">Show</a></td>
                <td><a href="/items/{{this.id}}/edit" style="background-color: green;">edit</a></td>
                <td>
            <form action="/items/{{this.id}}/delete" method="POST"
            onsubmit="return confirm('Are you sure you want to delete this item?');" style="background-colorr:red;border: none!important ">
            <button type="submit">
                Delete
            </button>
            </form>
            </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>